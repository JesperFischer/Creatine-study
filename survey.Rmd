---
title: "person data"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)

```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.


```{r}

d = read.delim("Sportsa.csv", sep="\t")
#maxp2 = read.delim("redcap_final_participants (1).csv", sep="	")
#maxp4 = read.csv("test123.csv")

#final = read.csv("participants_final.csv")



d = d[-c(1,2,3),]

d$rows = seq(1,62)

f = d %>% filter(d$k_n2 == 2)
m = d %>% filter(d$k_n2 == 1)

maxp = d %>% filter(d$max_pull_up2 > 5 & d$kreatin_2 != 1 & d$alder2 > 18)

maxp = maxp %>% filter(rows != 23 & rows != 27 & rows !=54 & rows !=22)

qqqq = maxp %>% filter(!ï..participant_id %in% final$ï..participant_id)
qqqq = qqqq %>% filter(klatre_r2 > 1)
final$X = NULL
final.v2 = rbind(final,qqqq)

#write.csv(final.v2, "participants_final.v2.csv")

#?write.csv()

maxp %>% group_by(k_n2) %>% summarize(n())

ka = d %>% filter(d$klatre_r2 > 1)



q = d %>% filter(max_pull_up2 > 3 & kreatin_2 != 1)
q = q %>% filter(max_pull_up2 < 28 & rows != 24)


q1 = q %>% filter(max_pull_up2 > 11 & rows != 6 & rows != 7 & rows != 12)



q2 = q %>% filter(!rows %in% q1$rows)



mos = maxp %>% filter(rows == 23 | rows == 27 | rows ==54 | rows ==22))


```

